version: '3.8'

services:
  app:
    build:
      context: .
      target: development
    container_name: python-tg-app
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: python -m src
    restart: unless-stopped

  # Example: Add database service if needed
  # db:
  #   image: postgres:15-alpine
  #   container_name: python-tg-db
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: dbname
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data:

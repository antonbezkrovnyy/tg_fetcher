# python-tg production environment example

# Redis
REDIS_URL=redis://tg-redis:6379
# Leave empty per MVP (no auth); set if you enable Redis auth
REDIS_PASSWORD=

# Service
ENVIRONMENT=production
LOG_LEVEL=DEBUG

# Scheduler settings (keep as-is unless changed)
FETCH_SCHEDULE_CHATS=ru_python
SCHEDULE_TIME=06:00
TIMEZONE=Europe/Moscow
RUN_ON_START=false

# Telegram Session
# Place session files under ./sessions (mounted to /app/sessions)
# API_ID and API_HASH are optional if session file is present
# TELEGRAM_API_ID=
# TELEGRAM_API_HASH=
